# Compliance OS — deployed app
Compiled static build of the Compliance OS admin (source is private).
Served via GitHub Pages. Contains only the public Supabase URL + anon key
(RLS enforces all data access).
